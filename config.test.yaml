# Alfred 测试环境配置
database:
  # auto cleanup by cleanup_test_db fixture
  path: "test_tasks.db"  # 使用测试数据库文件

logging:
  level: "DEBUG"

# better to set before tests?
# scheduler:
#   interval_seconds: 1
#   escalator_interval_seconds: 1
